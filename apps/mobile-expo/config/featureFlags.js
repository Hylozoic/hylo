// Feature flags configuration
// These can be overridden via environment variables with the FEATURE_FLAG_ prefix

export default {
  // Add feature flags here as needed
  // Example: enableNewFeature: true
} 