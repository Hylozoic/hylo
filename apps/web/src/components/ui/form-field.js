/** Field chrome shared by the group creation modal and the space create/edit modals,
 * so all three read as one family of forms. */
export const INPUT_CLASS = 'w-full rounded-lg border-2 border-foreground/20 bg-input px-3 py-2.5 text-sm text-foreground placeholder-foreground/40 focus:outline-none focus:border-focus transition-colors'

export const FIELD_LABEL_CLASS = 'text-xs font-bold text-foreground/80'
